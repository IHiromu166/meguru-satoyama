# AGENTS.md

このリポジトリで作業する AI エージェント (Codex / Claude Code など) への指示。

**2026-08-23 に Claude Code と Codex の分業をやめた。** 以降、作業者は常に1体で、
メインツリー `d:\git\game` の `main` 上で直接作業する。
worktree による分離とトラック分担 (トラックA / トラックB) は廃止済み。

## 言語

- ユーザーへの応答は日本語で行う。
- コミットメッセージは日本語で書く(1行目は変更の要約、50〜72文字程度)。
- コード内の識別子(変数名・関数名)は英語、コメントは日本語で書く。

## Git 運用

- 作業開始前に `git status` と `git log --oneline -5` で現在の状態を確認する。
- **`main` で直接コミットする。** 差し戻す可能性のある大きな試行だけトピックブランチを切り、
  終わったら `main` にマージして消す。`claude/*` `codex/*` の命名はもう使わない。
- コミットは意味のある単位で小さく分ける。無関係な変更を1コミットに混ぜない。
- 明示的な指示がない限り、以下を実行しない:
  - `git push` / `git push --force`(リモートへの反映はユーザーの判断で行う)
  - `git reset --hard` / `git checkout --` による変更の破棄
  - `git commit --amend`、履歴の書き換え(rebase 含む)
  - ブランチの削除、`git worktree remove`
- 自分が作っていないコミットやステージ済み変更を巻き込まない。見覚えのない変更が
  ワーキングツリーにある場合は、破棄せずそのまま残し、ユーザーに報告する。

## 作業範囲

- **依頼されたタスクの範囲外のファイルを変更しない。** 気になる箇所を見つけても、
  勝手に直さず報告に留める。
- **リポジトリ全体への一括操作をしない。** フォーマッタの全ファイル適用、一括リネーム、
  大規模リファクタは、明示的に指示された場合のみ行う(diff が膨らみレビューできなくなる)。
- 共有設定ファイル(lockfile、`.gitignore`、ビルド設定など)の変更は最小限にし、
  変更した場合は完了報告で必ず言及する。

## 検証と報告

- 変更後は可能な限り動作確認(テスト実行・起動確認)を行ってから完了とする。
- 確認していないことを「動作する」と報告しない。未検証なら未検証と明記する。
- テストやビルドが失敗したまま作業を終える場合は、失敗内容をそのまま報告する。
- 仕様と食い違った箇所、判断に迷って独断で決めた箇所は必ず報告に含める。

## ファイルとシークレット

- 一時ファイル・実験用スクリプトをリポジトリ内に残さない(作ったら消す)。
- API キー、トークン、パスワード等をコードやコミットに含めない。
  必要な場合は環境変数か `.env`(gitignore 対象)を使う。
- `.env` やクレデンシャルファイルの内容を読み取っても、出力・ログ・コミットに含めない。

## ドキュメント

- 挙動や使い方が変わる変更をしたら、関連ドキュメント(README 等)も同じ作業内で更新する。
- 実装の状態が変わったら [docs/STATUS.md](docs/STATUS.md) を更新する。ここが唯一の現在地。
- 設計上の重要な判断をした場合は、コミットメッセージまたは docs/ に理由を残す。

---

## プロジェクト固有

### 作るもの

**巡る里山** — デッキの循環がそのまま生態系の物質循環になっているソロ用デッキ構築ゲーム。
ブラウザ (Vite + TypeScript)、20ターン耐久 + 最終スコア。

現状は**遊べる状態**。残っているのはバランス調整・テストの穴埋め・実機での最終確認。

### 着手前に読む

| 文書 | 内容 |
| --- | --- |
| [docs/STATUS.md](docs/STATUS.md) | **最初に読む。** 現在どこまで出来ていて、何が未検証か |
| [docs/TASKS.md](docs/TASKS.md) | 残っている作業の詳細と、バランス調整の観点 |
| [docs/DESIGN.md](docs/DESIGN.md) | ゲームのルールと、実装時に迷う箇所の裁定 |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | 型定義、モジュール構成、core の公開 API |
| [docs/CARDS.md](docs/CARDS.md) | 全30種のカードデータ (数値は調整前の初期値) |
| [docs/MOBILE.md](docs/MOBILE.md) | スマートフォンでの動作確認手順と `vite.config.ts` の必須設定 |
| [docs/REVIEW.md](docs/REVIEW.md) | レビュー指摘16件と対応状況 |

### この企画でのルール

- `docs/ARCHITECTURE.md` の型定義と `docs/CARDS.md` の数値は**実装との契約**。
  実装しながら黙って変えない。変えるなら該当文書を同じ作業内で更新する。
- バランス調整(カードの数値変更)は、通しプレイでの検証とセットで行う。
- **ルール由来の数値を UI 側に書かない。** 判定も定数も `src/core/` から取る。
- 動作確認はスマートフォン(幅 360px の縦画面)を基準にする。
- 外部フォント・外部ライブラリ・**画像素材ファイル**を追加しない。
  カードの絵柄は `src/ui/art.ts` がその場で組み立てる SVG で描く (2026-08-24 に追加)。

### 動かし方

```
npm install
npm run dev        # http://127.0.0.1:5180 — PC のブラウザで見る (HMR あり)
npm run dev:tunnel # スマホから tailscale serve (:8443) 経由で見るとき
npm run build      # tsc -b && vite build
npm test           # vitest run
```

幅 360px の自動確認 (ヘッドレス Edge を CDP で叩く手順) は [docs/STATUS.md](docs/STATUS.md)、
実機での確認経路は [docs/MOBILE.md](docs/MOBILE.md) に書いてある。
