# AGENTS.md

このリポジトリで作業するすべての AI エージェント(Codex / Claude Code など)への共通指示。
プロジェクト固有の内容(ビルド方法・アーキテクチャ等)は決まり次第、下部の「プロジェクト固有」セクションに追記する。

## 言語

- ユーザーへの応答は日本語で行う。
- コミットメッセージは日本語で書く(1行目は変更の要約、50〜72文字程度)。
- コード内の識別子(変数名・関数名)は英語、コメントは日本語で書く。

## Git 運用

- 作業開始前に `git status` と `git log --oneline -5` で現在の状態を確認する。
- コミットは意味のある単位で小さく分ける。無関係な変更を1コミットに混ぜない。
- 明示的な指示がない限り、以下を実行しない:
  - `git push --force` / `git reset --hard` / `git checkout --` による変更の破棄
  - `git commit --amend`(他エージェントが参照している可能性がある)
  - ブランチの削除、履歴の書き換え(rebase 含む)
- 自分が作っていないコミットやステージ済み変更を巻き込まない。見覚えのない変更が
  ワーキングツリーにある場合は、破棄せずそのまま残し、ユーザーに報告する。

## 複数エージェントの同時作業

このディレクトリは複数の AI エージェントが同時に触ることがある。以下を厳守する:

- **依頼されたタスクの範囲外のファイルを変更しない。** 気になる箇所を見つけても、
  勝手に直さず報告に留める。
- **リポジトリ全体への一括操作をしない。** フォーマッタの全ファイル適用、一括リネーム、
  大規模リファクタは、明示的に指示された場合のみ行う(diff が膨らみ衝突の温床になる)。
- 他エージェントの作業中と思われる変更(自分が触っていない未コミットの差分)を
  上書き・削除・コミットしない。
- 共有設定ファイル(lockfile、.gitignore、CI 設定など)の変更は最小限にし、
  変更した場合は完了報告で必ず言及する。

## git worktree による作業分離(推奨運用)

同一ファイルへの物理的な書き込み衝突を防ぐため、エージェントごとに worktree を分けて作業する。

### 構成

- `d:\git\game` … メインの作業ツリー(main ブランチ)。統合・レビュー・マージ専用とし、
  エージェントはここで直接作業しない。
- `d:\git\game-codex` … Codex 用 worktree
- `d:\git\game-claude` … Claude Code 用 worktree

worktree はリポジトリの**外**(兄弟ディレクトリ)に置く。リポジトリ内に作ると
もう一方のエージェントの検索・一括操作に巻き込まれる。

### 手順

1. 初回セットアップ(メインツリーで実行):

   ```
   git worktree add ../game-codex -b codex/setup main
   git worktree add ../game-claude -b claude/setup main
   ```

2. 各エージェントは**自分の worktree ディレクトリで起動**し、そこだけで作業する。
   作業を始めるときは、最新の `main` を起点に `codex/<トピック>` /
   `claude/<トピック>` ブランチを作成する。`codex/main` / `claude/main` は作成しない。

   ```
   git switch -c codex/<トピック> main
   ```

   `main` はメイン worktree でチェックアウトされているため、エージェント用 worktree では
   直接チェックアウトしない。

3. 統合はメインツリー(`d:\git\game`)で行う:

   ```
   git merge codex/<トピック>
   git merge claude/<トピック>
   ```

   コンフリクトの解消はメインツリーで、ユーザーまたはユーザーが指名した
   エージェント1体のみが行う。

4. 不要になった worktree の削除(メインツリーで実行):

   ```
   git worktree remove ../game-codex
   git branch -d codex/<トピック>
   ```

   現状の確認は `git worktree list`。

### 注意点

- **同じブランチを複数の worktree で同時にチェックアウトすることはできない**
  (git がエラーにする)。エージェントごとに必ず別ブランチを使う。
- worktree 側の `.git` は本体を指すただのファイル。worktree が残っている間は
  メインリポジトリを移動・削除しない。
- **git 管理外のファイルは worktree に引き継がれない。** `.env` や `node_modules` 等の
  依存物は worktree ごとにコピー・再インストールが必要。
- 各エージェントは**自分以外の worktree に一切触れない。** 読み取りも含め、
  他エージェントの worktree ディレクトリを操作対象にしない。
- `git worktree remove` や `git worktree prune` は、ユーザーの指示があった場合のみ実行する。
- worktree を手動でディレクトリ削除してしまった場合は、メインツリーで
  `git worktree prune` を実行して登録情報を掃除する。

## 検証と報告

- 変更後は可能な限り動作確認(テスト実行・起動確認)を行ってから完了とする。
- 確認していないことを「動作する」と報告しない。未検証なら未検証と明記する。
- テストやビルドが失敗したまま作業を終える場合は、失敗内容をそのまま報告する。

## ファイルとシークレット

- 一時ファイル・実験用スクリプトをリポジトリ内に残さない(作ったら消す)。
- API キー、トークン、パスワード等をコードやコミットに含めない。
  必要な場合は環境変数か `.env`(gitignore 対象)を使う。
- `.env` やクレデンシャルファイルの内容を読み取っても、出力・ログ・コミットに含めない。

## ドキュメント

- 挙動や使い方が変わる変更をしたら、関連ドキュメント(README 等)も同じ作業内で更新する。
- 設計上の重要な判断をした場合は、コミットメッセージまたは docs/ に理由を残す。

---

## プロジェクト固有(随時追記)

### 作るもの

**巡る里山** — デッキの循環がそのまま生態系の物質循環になっているソロ用デッキ構築ゲーム。
ブラウザ (Vite + TypeScript)、20ターン耐久 + 最終スコア、制作期間1週間。

### 着手前に読む

コードを書き始める前に、以下を必ず読むこと。

| 文書 | 内容 |
| --- | --- |
| [docs/TASKS.md](docs/TASKS.md) | **最初に読む。** 作業分担、ファイル所有権、着手の順序 |
| [docs/DESIGN.md](docs/DESIGN.md) | ゲームのルールと、実装時に迷う箇所の裁定 |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | 型定義、モジュール構成、core の公開 API |
| [docs/CARDS.md](docs/CARDS.md) | 全30種のカードデータ (数値は調整前の初期値) |
| [docs/MOBILE.md](docs/MOBILE.md) | スマートフォンでの動作確認手順と `vite.config.ts` の必須設定 |
| [docs/REVIEW.md](docs/REVIEW.md) | レビューで出た未対応の指摘と担当。着手前に自分の担当分を確認する |

### この企画での追加ルール

- **フェイズ0 (雛形と型定義) が main にマージされるまで、他のエージェントは着手しない。**
  型の食い違いで成果が捨てになるため。
- `docs/ARCHITECTURE.md` の型定義と `docs/CARDS.md` の数値は **エージェント間の契約**。
  実装しながら勝手に変えない。変えたい場合は実装を止めて提案する。
- バランス調整 (カードの数値変更) はフェイズ2でのみ行う。
- 動作確認はスマートフォン (幅 360px の縦画面) を基準にする。

<!-- ビルド・実行コマンド、ディレクトリ構成、技術スタック、テストの実行方法など、
     プロジェクトの方向性が決まり次第ここに追記する -->
