import type { GameState } from "./types";

export function resolveInvasion(_state: GameState): GameState {
  throw new Error("未実装");
}

export function isCollapsed(_state: GameState): boolean {
  throw new Error("未実装");
}
