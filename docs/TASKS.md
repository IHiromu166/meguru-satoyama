# 巡る里山 — 残りの作業

現在どこまで出来ているかは [STATUS.md](./STATUS.md)。このファイルには**やることだけ**を書く。
ルールは [DESIGN.md](./DESIGN.md)、型と構成は [ARCHITECTURE.md](./ARCHITECTURE.md)、
カードの数値は [CARDS.md](./CARDS.md)。作業規約は [../AGENTS.md](../AGENTS.md) が優先する。

> **2026-08-23**: Claude Code と Codex の分業を終了した。
> かつてここにあった「フェイズ0 / トラックA / トラックB」の分担表とファイル所有権は廃止。
> 実装フェイズはすべて完了しており、以下は完成までの残件である。
> 当時の分担内容が要る場合は `git show a14f269:docs/TASKS.md` で読める。

優先度順。1と2は「壊れていないことの担保」、3が本丸。

---

## 1. シード固定で20ターン完走する通しテスト

`tests/` に1本追加する。**`survived` (20ターン到達) ルートを一度も通していない**ため、
これが生存側の分岐を踏む唯一の手段になる。

- シードを固定し、`createGame` から `advancePhase` を回して20ターンを走らせる。
- 途中でクラッシュしないこと、最終的に `survived` か `collapsed` のどちらかで
  停止することを確認する。
- 崩壊で終わるシードしか見つからない場合は、崩壊を避ける手を打つ操作列を
  テスト内に書いてでも生存ルートを1回通す。

## 2. テストの穴埋め

現状 `tests/core.test.ts` は13件。次が未カバー。

- `gainCard` — コスト不足の拒否、供給の残数の減少、捨て札への追加。
- `advancePhase` の解決順序 — [ARCHITECTURE.md 第6節](./ARCHITECTURE.md) のとおりか。
- クリーンアップでの `eatConsumer` の廃棄と、手札・場の捨て札送り。

## 3. バランス調整

**[CARDS.md](./CARDS.md) の数値は設計時の初期値のまま、一度も動かしていない。**
人が実際に20ターン遊び、その結果に基づいて調整する。
数値を変えたら CARDS.md を同じ作業内で更新する。

### 調整の観点

- 序盤 (1–8ターン) にエンジンが立ち上がるか。立たないなら初期コストが高すぎる
- 中盤 (9–14ターン) に外来種の対処と生態系の拡張がトレードオフになっているか
- 終盤 (15–20ターン) に手を打たないと崩壊するか。崩壊しないなら侵入圧が緩い
- 頂点捕食者が「到達できるが簡単ではない」位置にあるか
- 駆除カードに頼るルートが、在来天敵ルートより最終スコアで下回るか
  (下回らないなら「駆除のジレンマ」が成立していない)

## 4. 実機での通しプレイ

実機で見たのは phase 0 の「文字が出るだけ」の画面まで。**現在の UI を実機で通していない。**
接続経路は [MOBILE.md](./MOBILE.md)、幅 360px のヘッドレス確認手順は [STATUS.md](./STATUS.md)。
PWA のホーム画面追加とオフライン起動も、ここで初めて実機で確認する。

## 5. REVIEW.md C-7 の整理

- `src/core/state.ts` の `supplyOf` と `src/data/supply.ts` の `createSupply` が二重化している。
- `findCard` は未使用の export。`moveCard` も実装から呼ばれていない。

どちらを残すかを決めて片方に寄せる。[REVIEW.md](./REVIEW.md) の残件はこれとテストの穴だけ。

## 6. ARCHITECTURE.md の追補

`src/core/cards.ts` の `cardDefinition` と `invasionPressure` が、
第2節のモジュール構成表と第4節の公開 API 一覧に載っていない。

## 7. (任意) Electron 化

やらなくても完成する。ブラウザ + PWA で足りているなら見送ってよい。

---

## 完了報告に含めること

- 変更したファイルの一覧
- **検証したこと / していないこと。** 動かしていないものを「動く」と書かない
- 仕様と食い違った箇所、判断に迷って独断で決めた箇所
- 直したくなったが範囲外なので触らなかった箇所
